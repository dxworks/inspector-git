name: IGlog
run: onEach
results:
  - dir: ${instrument}/results
    files:
      - "*.iglog"
commands:
  - name: extract iglogs
    win: java -jar iglog.jar ${repo}
    unix: java -jar iglog.jar ${repo}
